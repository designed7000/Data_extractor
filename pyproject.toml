[project]
name = "data-extractor"
version = "0.1.0"
description = "GenAI data extraction project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

